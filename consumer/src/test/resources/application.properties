# override the baseUrl to use the stub server
baseUrl=http://localhost:9898

# what stubs to load
stubrunner.ids=org.jab.microservices:producer:+:9898

# use local dependencies
stubrunner.work-offline=true